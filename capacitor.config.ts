import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.lovable.musabaqat',
  appName: 'al-tarikh-al-saheeh',
  webDir: 'dist'
};

export default config;
